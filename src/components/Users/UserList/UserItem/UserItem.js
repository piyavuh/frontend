import React from "react";
import "./UserItem.css";
import { Table, Button } from "antd";
const userItem = (props) => {
 
  return (
    <div>
      {props.First_name}
     
    </div>
  );
};

export default userItem;
